/** Streak flame icon — static. Personality here comes from the copy around it, not motion. */
export function StreakFlame() {
  return <span className="text-lg">🔥</span>;
}

export default StreakFlame;
