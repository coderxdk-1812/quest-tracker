{
  "_comment": "Tiny CPU run on a handful of examples — proves the training pipeline works end-to-end (tokenize, forward/backward, eval, checkpoint) without needing a GPU. Not a real training run.",
  "model_name": "google/flan-t5-small",
  "train_file": "data/processed/train.jsonl",
  "val_file": "data/processed/val.jsonl",
  "output_dir": "runs/smoke",
  "max_source_length": 256,
  "max_target_length": 256,
  "per_device_train_batch_size": 2,
  "per_device_eval_batch_size": 2,
  "gradient_accumulation_steps": 1,
  "learning_rate": 5e-4,
  "num_train_epochs": 2,
  "max_train_samples": 12,
  "max_val_samples": 6,
  "eval_strategy": "steps",
  "eval_steps": 3,
  "save_steps": 3,
  "save_total_limit": 2,
  "logging_steps": 1,
  "fp16": false,
  "bf16": false,
  "seed": 42,
  "warmup_ratio": 0.0,
  "weight_decay": 0.0,
  "lr_scheduler_type": "constant",
  "generation_max_length": 256,
  "generation_num_beams": 1,
  "no_cuda": true
}
