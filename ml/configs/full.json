{
  "_comment": "Full training run on the complete dataset. NEEDS A GPU (local CUDA or Colab) — see ml/README.md for how to launch it. fp16 assumes CUDA; set fp16=false and bf16=false if running on CPU/MPS only (much slower).",
  "model_name": "google/flan-t5-small",
  "train_file": "data/processed/train.jsonl",
  "val_file": "data/processed/val.jsonl",
  "output_dir": "runs/full",
  "max_source_length": 384,
  "max_target_length": 384,
  "per_device_train_batch_size": 16,
  "per_device_eval_batch_size": 16,
  "gradient_accumulation_steps": 2,
  "learning_rate": 3e-4,
  "num_train_epochs": 8,
  "max_train_samples": null,
  "max_val_samples": null,
  "eval_strategy": "epoch",
  "save_total_limit": 3,
  "logging_steps": 50,
  "fp16": true,
  "bf16": false,
  "seed": 42,
  "warmup_ratio": 0.05,
  "weight_decay": 0.01,
  "lr_scheduler_type": "linear",
  "generation_max_length": 384,
  "generation_num_beams": 4,
  "no_cuda": false
}
