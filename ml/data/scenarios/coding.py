"""25 distinct coding scenarios — wide languages, project types, and build-vs-debug modes."""

from __future__ import annotations

import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).parent.parent))
from ._common import Scenario, no_post  # noqa: E402

SCENARIOS = [
    Scenario(
        id="coding_rust_borrow_checker",
        intent="coding",
        title_tpl="fighting the rust borrow checker, debugging existing code",
        description_tpl=None,
        var_pools={"subject": ["Computer Science"]},
        breakdown_steps_tpl=[
            "Copy the exact compiler error message, not just a summary of it",
            "Identify which variable ownership rule is actually being violated (move, borrow, lifetime)",
            "Check whether you actually need ownership there, or a reference (&) would work instead",
            "Try the smallest possible fix (add a reference, a clone, or restructure the scope) and recompile",
            "If cloning fixed it, decide whether that's actually fine here or whether it's masking a bigger design issue",
            "Recompile the whole project once more to confirm no new borrow errors appeared elsewhere",
        ],
        vague_indices=[2],
        clarify_questions_tpl=[{"question": "What's the exact compiler error — a move error, a borrow conflict, or a lifetime error?", "target_index": 2}],
        refine_answers_tpl=["value moved here, then used again later, it's the classic move error"],
        refine_steps_tpl=[
            "Copy the exact compiler error message, not just a summary of it",
            "Confirm it's a move error: a value used after being moved, meaning ownership transferred and the original binding is no longer valid",
            "Check whether the second use actually needs ownership, or whether passing a reference (&value) the first time would avoid the move entirely",
            "Try passing a reference instead of the value where possible, and recompile",
            "If a reference genuinely won't work there, use .clone() instead, but note it as a spot to revisit for a cleaner design later",
            "Recompile the whole project once more to confirm no new borrow errors appeared elsewhere",
        ],
    ),
    Scenario(
        id="coding_todo_flask",
        intent="coding",
        title_tpl="Build a to-do list app in Python (Flask)",
        description_tpl="{subject}, new build.",
        var_pools={"subject": ["Computer Science"]},
        breakdown_steps_tpl=[
            "Restate what the to-do app needs: add, complete, delete a task, and persist it",
            "Sketch the routes: GET /tasks, POST /tasks, PATCH /tasks/id, DELETE /tasks/id",
            "Scaffold the Flask project and implement just the in-memory version first",
            "Add SQLite persistence once the in-memory version works",
            "Test each route with a simple case, then edge cases (empty title, missing id)",
            "Clean up and add a comment on how the routing works",
        ],
        vague_indices=[2],
        clarify_questions_tpl=[{"question": "Should the first version use in-memory storage or a real database right away?", "target_index": 2}],
        refine_answers_tpl=["in-memory first, I want to get the routes working before touching a database"],
        refine_steps_tpl=[
            "Restate what the to-do app needs: add, complete, delete a task, and persist it",
            "Sketch the routes: GET /tasks, POST /tasks, PATCH /tasks/id, DELETE /tasks/id",
            "Scaffold the Flask project with a plain Python list/dict as in-memory storage — no database yet, just get the routes working end to end",
            "Add SQLite persistence once the in-memory version works",
            "Test each route with a simple case, then edge cases (empty title, missing id)",
            "Clean up and add a comment on how the routing works",
        ],
    ),
    Scenario(
        id="coding_portfolio_site",
        intent="coding",
        title_tpl="build my portfolio website, new build",
        description_tpl=None,
        var_pools={"subject": ["Computer Science"]},
        breakdown_steps_tpl=[
            "List the sections you need: hero, projects, about, contact",
            "Sketch a rough layout on paper before touching code",
            "Scaffold the HTML/CSS structure for just the hero and nav first",
            "Build out the projects section with 2-3 real project cards",
            "Make it responsive — test at mobile width",
            "Deploy it and check every link actually works",
        ],
        vague_indices=[2],
        clarify_questions_tpl=[{"question": "Are you using a framework, or plain HTML/CSS/JS?", "target_index": 2}],
        refine_answers_tpl=["plain HTML/CSS/JS, I don't want the overhead of a framework for something this small"],
        refine_steps_tpl=[
            "List the sections you need: hero, projects, about, contact",
            "Sketch a rough layout on paper before touching code",
            "Scaffold plain HTML/CSS for just the hero and nav first — no framework, since it's overkill for a site this size",
            "Build out the projects section with 2-3 real project cards using plain CSS grid/flexbox",
            "Make it responsive — test at mobile width",
            "Deploy it and check every link actually works",
        ],
    ),
    Scenario(
        id="coding_debug_python_crash",
        intent="coding",
        title_tpl="my python script keeps crashing, need to fix it",
        description_tpl=None,
        var_pools={"subject": ["Computer Science"]},
        breakdown_steps_tpl=[
            "Reproduce the crash reliably and note the exact error message and line number",
            "Read the traceback from the bottom up to find where it actually originates",
            "Add print statements or a debugger breakpoint right before the failing line",
            "Narrow down which specific input value triggers the crash",
            "Fix it, then re-run the original reproduction case",
            "Add a test so this input can't crash it again silently",
        ],
        vague_indices=[2],
        clarify_questions_tpl=[{"question": "What's the actual error message and line number from the traceback?", "target_index": 2}],
        refine_answers_tpl=["IndexError on line 42, looks like it's trying to access a list that's sometimes empty"],
        refine_steps_tpl=[
            "Reproduce the crash reliably and note the exact error message and line number",
            "Read the traceback from the bottom up to find where it actually originates",
            "On line 42, check whether the list can legitimately be empty at that point — add a length check or default value there since that's the confirmed IndexError source",
            "Narrow down which specific input causes the list to be empty in the first place",
            "Fix it, then re-run the original reproduction case",
            "Add a test with an empty-list input so this can't crash it again silently",
        ],
    ),
    Scenario(
        id="coding_discord_bot",
        intent="coding",
        title_tpl="Build a Discord bot in Python",
        description_tpl="{subject}, new build.",
        var_pools={"subject": ["Computer Science"]},
        breakdown_steps_tpl=[
            "Restate what the bot needs to do: which commands, which server events it reacts to",
            "Set up the bot application and get a token, testing that it can connect at all",
            "Scaffold the project and implement the simplest command first (e.g. a ping command)",
            "Add the remaining commands one at a time, testing each in a real server",
            "Handle the case where a command is used with bad/missing arguments",
            "Clean up and document each command's usage",
        ],
        vague_indices=[2],
        clarify_questions_tpl=[{"question": "What should the bot's very first working command actually be?", "target_index": 2}],
        refine_answers_tpl=["a command that rolls dice for our D&D group, that's the whole reason I'm building this"],
        refine_steps_tpl=[
            "Restate what the bot needs to do: which commands, which server events it reacts to",
            "Set up the bot application and get a token, testing that it can connect at all",
            "Scaffold the project and implement a dice-roll command first (e.g. !roll 2d6), since that's the actual reason for building this bot",
            "Add the remaining commands one at a time, testing each in a real server",
            "Handle bad dice notation (e.g. !roll banana) gracefully with an error message",
            "Clean up and document each command's usage",
        ],
    ),
    Scenario(
        id="coding_java_memory_leak",
        intent="coding",
        title_tpl="fix a memory leak in my java app",
        description_tpl=None,
        var_pools={"subject": ["Computer Science"]},
        breakdown_steps_tpl=[
            "Reproduce the leak reliably — find an action that grows memory usage every time it's repeated",
            "Take a heap dump before and after repeating that action several times",
            "Compare the dumps to find which object type is accumulating",
            "Trace back to what's holding a reference to those objects and preventing garbage collection",
            "Fix the reference leak (e.g. unregister a listener, close a resource), then re-test",
            "Add a test or monitoring check so this regression is caught earlier next time",
        ],
        vague_indices=[2],
        clarify_questions_tpl=[{"question": "What action seems to correlate with memory growing?", "target_index": 2}],
        refine_answers_tpl=["opening and closing a settings dialog repeatedly, memory keeps climbing"],
        refine_steps_tpl=[
            "Reproduce the leak by opening and closing the settings dialog repeatedly, confirming memory climbs each time",
            "Take a heap dump before and after repeating that dialog open/close several times",
            "Compare the dumps to find which object type is accumulating — likely the dialog's own listeners or view objects",
            "Check whether the dialog registers a listener (e.g. on the main window) that it never unregisters on close — that's the usual cause of this exact pattern",
            "Fix the unregistered listener, then re-test the open/close cycle",
            "Add a test or monitoring check so this regression is caught earlier next time",
        ],
    ),
    Scenario(
        id="coding_pygame_game",
        intent="coding",
        title_tpl="Build a simple game in Pygame for my CS project",
        description_tpl="{subject}, new build.",
        var_pools={"subject": ["Computer Science"]},
        breakdown_steps_tpl=[
            "Restate the core game loop: what the player does, and what makes them win/lose",
            "Sketch the game states you need (menu, playing, game over)",
            "Scaffold the Pygame project and implement just a movable player sprite first",
            "Add collision detection between the player and one obstacle type",
            "Add the scoring/win condition once movement and collision both work",
            "Playtest it yourself 5 times and fix whatever feels broken or unfair",
        ],
        vague_indices=[2],
        clarify_questions_tpl=[{"question": "What kind of game is this — platformer, top-down, or something else?", "target_index": 2}],
        refine_answers_tpl=["top-down, like a simple dodge-the-obstacles game"],
        refine_steps_tpl=[
            "Restate the core game loop: player moves in 2D to dodge obstacles, loses on collision",
            "Sketch the game states you need (menu, playing, game over)",
            "Scaffold the Pygame project and implement a top-down movable player sprite (arrow keys, 4-directional) first",
            "Add collision detection between the player and one falling obstacle type",
            "Add the scoring/win condition (survive N seconds) once movement and collision both work",
            "Playtest it yourself 5 times and fix whatever feels broken or unfair",
        ],
    ),
    Scenario(
        id="coding_blog_api",
        intent="coding",
        title_tpl="REST API for a blog, node/express",
        description_tpl=None,
        var_pools={"subject": ["Computer Science"]},
        breakdown_steps_tpl=[
            "Restate the resources: posts, comments, and what CRUD operations each needs",
            "Sketch the route table before writing any code",
            "Scaffold the Express project and implement just the posts endpoints first",
            "Add the comments endpoints, nested under a post id",
            "Test every endpoint with a simple case, then edge cases (missing fields, invalid id)",
            "Add basic input validation and error responses",
        ],
        vague_indices=[2],
        clarify_questions_tpl=[{"question": "Are you using a real database, or an in-memory array to start?", "target_index": 2}],
        refine_answers_tpl=["in-memory array for now, I'll swap in MongoDB once the API shape is right"],
        refine_steps_tpl=[
            "Restate the resources: posts, comments, and what CRUD operations each needs",
            "Sketch the route table before writing any code",
            "Scaffold the Express project with an in-memory array as storage and implement just the posts endpoints first — swap to MongoDB later once the API shape is confirmed",
            "Add the comments endpoints, nested under a post id, still using the in-memory array",
            "Test every endpoint with a simple case, then edge cases (missing fields, invalid id)",
            "Add basic input validation and error responses",
        ],
    ),
    Scenario(
        id="coding_web_scraper",
        intent="coding",
        title_tpl="build a web scraper in python, new build",
        description_tpl=None,
        var_pools={"subject": ["Computer Science"]},
        breakdown_steps_tpl=[
            "Restate exactly what data you need to extract and from which pages",
            "Inspect the target page's HTML to find the right selectors",
            "Scaffold the script and get it working for a single page first",
            "Add pagination/looping to cover multiple pages",
            "Handle missing elements gracefully instead of crashing on the first exception",
            "Save the results to a CSV/JSON file and spot-check a few rows",
        ],
        vague_indices=[2],
        clarify_questions_tpl=[{"question": "What library are you using — requests+BeautifulSoup, or something like Selenium for JS-rendered pages?", "target_index": 2}],
        refine_answers_tpl=["requests and BeautifulSoup, the site is static HTML so I don't need a browser"],
        refine_steps_tpl=[
            "Restate exactly what data you need to extract and from which pages",
            "Inspect the target page's HTML to find the right selectors",
            "Scaffold the script using requests + BeautifulSoup (no browser needed since the site is static HTML) and get it working for a single page first",
            "Add pagination/looping to cover multiple pages",
            "Handle missing elements gracefully instead of crashing on the first exception",
            "Save the results to a CSV/JSON file and spot-check a few rows",
        ],
    ),
    Scenario(
        id="coding_swift_mobile_app",
        intent="coding",
        title_tpl="Build a habit tracker app in Swift",
        description_tpl="{subject}, new build.",
        var_pools={"subject": ["Computer Science"]},
        breakdown_steps_tpl=[
            "Restate the core feature: add a habit, mark it done today, see a streak count",
            "Sketch the two screens you need (list view, add-habit view)",
            "Scaffold the Xcode project and implement just the list view with hardcoded data first",
            "Wire up the add-habit screen and connect it to real (in-memory) data",
            "Add persistence with UserDefaults or CoreData once the UI flow works",
            "Test on both a small and large simulator screen size",
        ],
        vague_indices=[2],
        clarify_questions_tpl=[{"question": "SwiftUI or UIKit for this?", "target_index": 2}],
        refine_answers_tpl=["SwiftUI, it's what we're learning in class right now"],
        refine_steps_tpl=[
            "Restate the core feature: add a habit, mark it done today, see a streak count",
            "Sketch the two screens you need (list view, add-habit view)",
            "Scaffold the Xcode SwiftUI project and implement just the list view with hardcoded @State data first, since SwiftUI is what class is covering",
            "Wire up the add-habit SwiftUI view and connect it to real (in-memory) data via @State/@Binding",
            "Add persistence with UserDefaults once the SwiftUI flow works",
            "Test on both a small and large simulator screen size",
        ],
    ),
    Scenario(
        id="coding_fix_failing_tests",
        intent="coding",
        title_tpl="fix my failing unit tests before submitting",
        description_tpl=None,
        var_pools={"subject": ["Computer Science"]},
        breakdown_steps_tpl=[
            "Run the test suite and list every failing test by name",
            "Read the first failure's assertion message carefully — expected vs actual",
            "Decide whether the code is wrong or the test's expectation is wrong",
            "Fix the underlying cause, not just the assertion",
            "Re-run the full suite, not just the one test, to check for new breakage",
            "Repeat for each remaining failure, one at a time",
        ],
        vague_indices=[2],
        clarify_questions_tpl=[{"question": "For the first failing test — is the code producing the wrong output, or is the test's expected value actually incorrect?", "target_index": 2}],
        refine_answers_tpl=["the code, it's returning null when it should return an empty list"],
        refine_steps_tpl=[
            "Run the test suite and list every failing test by name",
            "Read the first failure's assertion message carefully — expected vs actual",
            "Confirm the code is wrong: it returns null where an empty list is expected — find where that null comes from",
            "Fix the function to return an empty list instead of null in that case, not just patch the test",
            "Re-run the full suite, not just the one test, to check for new breakage",
            "Repeat for each remaining failure, one at a time",
        ],
    ),
    Scenario(
        id="coding_chatbot",
        intent="coding",
        title_tpl="build a simple chatbot in python for cs class",
        description_tpl=None,
        var_pools={"subject": ["Computer Science"]},
        breakdown_steps_tpl=[
            "Restate what the chatbot needs to handle: a fixed set of intents, or open-ended input",
            "Sketch the intent categories and one example response for each",
            "Scaffold the project and implement keyword matching for just one intent first",
            "Add the remaining intents one at a time, testing each with several phrasings",
            "Add a fallback response for anything that doesn't match",
            "Clean up and test the whole conversation flow end to end",
        ],
        vague_indices=[2],
        clarify_questions_tpl=[{"question": "Is this keyword/rule-based, or are you meant to use an NLP library?", "target_index": 2}],
        refine_answers_tpl=["rule-based with keyword matching, the assignment specifically says no external NLP libraries"],
        refine_steps_tpl=[
            "Restate what the chatbot needs to handle: a fixed set of intents, or open-ended input",
            "Sketch the intent categories and one example response for each",
            "Scaffold the project and implement plain keyword matching (no external NLP libraries, per the assignment rules) for just one intent first",
            "Add the remaining intents one at a time using keyword matching, testing each with several phrasings",
            "Add a fallback response for anything that doesn't match any keyword",
            "Clean up and test the whole conversation flow end to end",
        ],
    ),
    Scenario(
        id="coding_sql_schema",
        intent="coding",
        title_tpl="Design and build the SQL schema for my project",
        description_tpl="{subject}, new build.",
        var_pools={"subject": ["Computer Science"]},
        breakdown_steps_tpl=[
            "List every entity the project needs (users, orders, products, etc.)",
            "Sketch the relationships between entities before writing any SQL",
            "Write the CREATE TABLE statements for the simplest entity first",
            "Add foreign keys and constraints as you add the related tables",
            "Insert sample data and write 2-3 test queries to check the schema actually supports your app's needs",
            "Add indexes on any column you'll frequently filter or join on",
        ],
        vague_indices=[2],
        clarify_questions_tpl=[{"question": "What are the actual entities your project needs to store?", "target_index": 2}],
        refine_answers_tpl=["users, recipes, and ingredients — it's a recipe-sharing app"],
        refine_steps_tpl=[
            "Confirm the entities: users, recipes, and ingredients for the recipe-sharing app",
            "Sketch the relationships: users own recipes, recipes have many ingredients (many-to-many via a join table)",
            "Write the CREATE TABLE statement for users first, the simplest entity",
            "Add recipes with a foreign key to users, then the ingredients join table with foreign keys to both recipes and ingredients",
            "Insert sample data and write test queries like "'"all recipes by a user"'" and "'"all ingredients in a recipe"'"",
            "Add indexes on the foreign key columns you'll join on frequently",
        ],
    ),
    Scenario(
        id="coding_ml_training_script",
        intent="coding",
        title_tpl="write a machine learning model training script in python",
        description_tpl=None,
        var_pools={"subject": ["Computer Science"]},
        breakdown_steps_tpl=[
            "Restate the task: what you're predicting, and what your dataset looks like",
            "Sketch the pipeline: load data, split train/test, train, evaluate",
            "Scaffold the script and get a baseline model (e.g. logistic regression) running end to end first",
            "Add proper preprocessing (scaling, encoding) once the baseline runs",
            "Evaluate with the right metric for your problem, not just accuracy blindly",
            "Save the trained model and write a short note on its performance",
        ],
        vague_indices=[2],
        clarify_questions_tpl=[{"question": "Is this classification or regression, and roughly how big is your dataset?", "target_index": 2}],
        refine_answers_tpl=["classification, predicting whether a customer churns, about 5000 rows and pretty imbalanced classes"],
        refine_steps_tpl=[
            "Restate the task: predicting customer churn (binary classification), ~5000 rows, imbalanced classes",
            "Sketch the pipeline: load data, split train/test (stratified, given the imbalance), train, evaluate",
            "Scaffold the script and get a baseline logistic regression running end to end first",
            "Add proper preprocessing (scaling, encoding) once the baseline runs",
            "Evaluate using precision/recall/F1 or ROC-AUC instead of raw accuracy, since class imbalance makes accuracy misleading here",
            "Save the trained model and write a short note on its performance",
        ],
    ),
    Scenario(
        id="coding_chrome_extension",
        intent="coding",
        title_tpl="Build a Chrome extension for a class project",
        description_tpl="{subject}, new build.",
        var_pools={"subject": ["Computer Science"]},
        breakdown_steps_tpl=[
            "Restate exactly what the extension should do when clicked or on page load",
            "Sketch the manifest.json permissions you'll actually need",
            "Scaffold the extension and get the popup UI showing with static content first",
            "Wire up the content script to actually interact with the page",
            "Test it on 2-3 different real websites, not just your test page",
            "Clean up permissions to only what's actually used before submitting",
        ],
        vague_indices=[2],
        clarify_questions_tpl=[{"question": "Does the extension need to modify the page content, or just show info in a popup?", "target_index": 2}],
        refine_answers_tpl=["modify the page — it highlights certain keywords on any page you're viewing"],
        refine_steps_tpl=[
            "Restate exactly what the extension should do: highlight certain keywords on the current page",
            "Sketch the manifest.json permissions you'll need — activeTab and scripting, since it modifies page content",
            "Scaffold the extension and get the popup UI showing with a static keyword list first",
            "Wire up a content script that scans the page DOM and wraps matching keywords in a highlight span",
            "Test the keyword highlighting on 2-3 different real websites, not just your test page",
            "Clean up permissions to only what's actually used before submitting",
        ],
    ),
    Scenario(
        id="coding_debug_react_render",
        intent="coding",
        title_tpl="my react component won't render, please help",
        description_tpl=None,
        var_pools={"subject": ["Computer Science"]},
        breakdown_steps_tpl=[
            "Check the browser console for any error first, not just a blank screen",
            "Confirm the component is actually being imported and used in the parent",
            "Add a simple console.log at the top of the component to check if it's even called",
            "Narrow down whether it's a rendering error, a conditional that's always false, or a missing return",
            "Fix it, then verify it renders with real props, not just mock data",
            "Remove the debug console.log before committing",
        ],
        vague_indices=[2],
        clarify_questions_tpl=[{"question": "Is there an actual error in the console, or does it just silently show nothing?", "target_index": 2}],
        refine_answers_tpl=["no console error at all, it just silently shows nothing"],
        refine_steps_tpl=[
            "Confirm there's no console error — this points to either a conditional render returning null, or the component simply not being reached",
            "Confirm the component is actually being imported and used in the parent",
            "Add a console.log at the very top of the component function to check if it's even being called at all",
            "If it IS called, check every conditional return (especially `return null` or an early `if`) for one that's silently always true",
            "Fix the faulty conditional, then verify it renders with real props, not just mock data",
            "Remove the debug console.log before committing",
        ],
    ),
    Scenario(
        id="coding_cli_tool_go",
        intent="coding",
        title_tpl="Build a CLI tool in Go for file organization",
        description_tpl="{subject}, new build.",
        var_pools={"subject": ["Computer Science"]},
        breakdown_steps_tpl=[
            "Restate exactly what the tool should do: sort files into folders by what criteria",
            "Sketch the command-line flags/arguments it needs",
            "Scaffold the Go project and implement just listing files in a directory first",
            "Add the actual sorting logic once file listing works",
            "Test it on a throwaway test directory, not your real files, until you trust it",
            "Add a --dry-run flag so it can preview changes before actually moving anything",
        ],
        vague_indices=[2],
        clarify_questions_tpl=[{"question": "What's the sorting criteria — file extension, date, or something else?", "target_index": 2}],
        refine_answers_tpl=["file extension, I want images, docs, and videos each in their own folder"],
        refine_steps_tpl=[
            "Restate exactly what the tool should do: sort files by extension into images/docs/videos folders",
            "Sketch the command-line flags/arguments it needs (target directory, maybe custom extension mappings)",
            "Scaffold the Go project and implement just listing files in a directory first",
            "Add the extension-based sorting logic (map extensions to folder names) once file listing works",
            "Test it on a throwaway test directory, not your real files, until you trust it",
            "Add a --dry-run flag so it can preview the file moves before actually moving anything",
        ],
    ),
    Scenario(
        id="coding_automation_script",
        intent="coding",
        title_tpl="automate a repetitive task with a python script",
        description_tpl=None,
        var_pools={"subject": ["Computer Science"]},
        breakdown_steps_tpl=[
            "Write down the exact manual steps you currently repeat, in order",
            "Identify which steps can be scripted vs which need a human decision",
            "Scaffold the script and automate the first scriptable step only",
            "Add the remaining steps one at a time, testing after each addition",
            "Handle the case where an expected file/input is missing, instead of crashing",
            "Run it on a real (but low-stakes) case to confirm it saves the time you expect",
        ],
        vague_indices=[2],
        clarify_questions_tpl=[{"question": "What's the repetitive task, specifically?", "target_index": 2}],
        refine_answers_tpl=["renaming and moving downloaded PDFs into folders by course name"],
        refine_steps_tpl=[
            "Write down the exact manual steps you currently repeat: check filename, identify course, rename, move to that course's folder",
            "Identify which steps can be scripted (rename, move) vs which need a human decision (ambiguous filenames)",
            "Scaffold the script and automate reading the downloads folder and printing detected course names first",
            "Add the rename-and-move logic once course detection works",
            "Handle filenames that don't match any known course pattern by leaving them alone and printing a warning, instead of crashing",
            "Run it on your real downloads folder once you trust it, and confirm it actually saves you time",
        ],
    ),
    Scenario(
        id="coding_weather_app_js",
        intent="coding",
        title_tpl="Build a weather app using a public API, JavaScript",
        description_tpl="{subject}, new build.",
        var_pools={"subject": ["Computer Science"]},
        breakdown_steps_tpl=[
            "Restate exactly what the app should show: current weather only, or a forecast too",
            "Sign up for the weather API and get a test request working in the browser/curl first",
            "Scaffold the page and implement fetching + displaying current weather for one hardcoded city",
            "Add a search input so the user can enter any city",
            "Handle the API returning an error (bad city name, rate limit) gracefully in the UI",
            "Style it and test on mobile width",
        ],
        vague_indices=[2],
        clarify_questions_tpl=[{"question": "Just current weather, or a multi-day forecast too?", "target_index": 2}],
        refine_answers_tpl=["just current weather for now, I can add forecast later if there's time"],
        refine_steps_tpl=[
            "Restate exactly what the app should show: current weather only, no forecast for now",
            "Sign up for the weather API and get a test request working in the browser/curl first",
            "Scaffold the page and implement fetching + displaying current weather for one hardcoded city",
            "Add a search input so the user can enter any city",
            "Handle the API returning an error (bad city name, rate limit) gracefully in the UI",
            "Style it and test on mobile width, leaving room to add a forecast section later if time allows",
        ],
    ),
    Scenario(
        id="coding_debug_c_segfault",
        intent="coding",
        title_tpl="segfault in my C program, debugging existing code",
        description_tpl=None,
        var_pools={"subject": ["Computer Science"]},
        breakdown_steps_tpl=[
            "Reproduce the segfault reliably with the smallest input that triggers it",
            "Run it under a debugger (gdb) or with valgrind to get the exact crashing line",
            "Check whether it's a null pointer dereference, out-of-bounds access, or use-after-free",
            "Narrow down why the pointer is invalid at that point by tracing backward",
            "Fix it, then re-run under valgrind again to confirm no other memory errors remain",
            "Add a bounds/null check where appropriate so this fails loudly instead of silently next time",
        ],
        vague_indices=[2],
        clarify_questions_tpl=[{"question": "What did gdb/valgrind actually report as the crashing line and error type?", "target_index": 2}],
        refine_answers_tpl=["valgrind says invalid read of size 4, looks like I'm accessing an array one past its end"],
        refine_steps_tpl=[
            "Reproduce the segfault reliably with the smallest input that triggers it",
            "Confirm from valgrind's "'"invalid read of size 4"'" that it's an out-of-bounds array access, one past the end",
            "Find the loop or index calculation that goes one too far (classic off-by-one, likely `<=` where it should be `<`)",
            "Fix the boundary condition, then re-run under valgrind again to confirm no other memory errors remain",
            "Test with an array of size 1 and size 0 as edge cases",
            "Add an assert or bounds check on that index going forward so this fails loudly instead of silently next time",
        ],
    ),
    Scenario(
        id="coding_arduino_iot",
        intent="coding",
        title_tpl="Arduino project — temperature monitor, new build",
        description_tpl="{subject}.",
        var_pools={"subject": ["Computer Science", "Engineering"]},
        breakdown_steps_tpl=[
            "Restate what the monitor should do: read temperature, display it, and alert if too high",
            "Wire up just the sensor and confirm you can read raw values in the serial monitor first",
            "Scaffold the code to convert raw sensor values to actual temperature units",
            "Add the display (LCD/LED) once the temperature reading is confirmed correct",
            "Add the alert logic (e.g. LED or buzzer) for the too-high case",
            "Test it across a real temperature range (e.g. holding it near something warm) to confirm the alert triggers correctly",
        ],
        vague_indices=[2],
        clarify_questions_tpl=[{"question": "Which sensor are you using, and have you confirmed you can read raw values from it yet?", "target_index": 2}],
        refine_answers_tpl=["a DHT22 sensor, and no, I haven't gotten any readings from it yet, not sure if it's wired right"],
        refine_steps_tpl=[
            "Restate what the monitor should do: read temperature, display it, and alert if too high",
            "Before anything else, verify the DHT22 wiring against its datasheet pinout and confirm you get ANY raw serial output — if not, check the pull-up resistor and power pin first, since that's the most common DHT22 wiring mistake",
            "Once raw values appear, scaffold the code to convert them to actual temperature units using the DHT library",
            "Add the display (LCD/LED) once the temperature reading is confirmed correct",
            "Add the alert logic (e.g. LED or buzzer) for the too-high case",
            "Test it across a real temperature range (e.g. holding it near something warm) to confirm the alert triggers correctly",
        ],
    ),
    Scenario(
        id="coding_refactor_legacy",
        intent="coding",
        title_tpl="refactor this legacy code before it gets worse, debugging existing code",
        description_tpl=None,
        var_pools={"subject": ["Computer Science"]},
        breakdown_steps_tpl=[
            "Make sure there are tests covering the current behavior before changing anything",
            "Identify the single worst offender (longest function, most duplication) to start with",
            "Extract one small piece into its own function without changing behavior",
            "Re-run the tests after every single small extraction, not just at the end",
            "Repeat the extract-and-test cycle for the next worst piece",
            "Document the new structure so the next person understands the reorganization",
        ],
        vague_indices=[2],
        clarify_questions_tpl=[{"question": "Are there existing tests covering this code, or none at all?", "target_index": 2}],
        refine_answers_tpl=["none at all, which is honestly why it's scary to touch"],
        refine_steps_tpl=[
            "Before refactoring anything, write a handful of characterization tests that just capture the CURRENT behavior (even if it's not ideal) — this is what makes it safe to touch",
            "Identify the single worst offender (longest function, most duplication) to start with",
            "Extract one small piece into its own function without changing behavior",
            "Re-run your new characterization tests after every single small extraction, not just at the end",
            "Repeat the extract-and-test cycle for the next worst piece",
            "Document the new structure so the next person understands the reorganization",
        ],
    ),
    Scenario(
        id="coding_recommendation_system",
        intent="coding",
        title_tpl="Build a simple recommendation system in Python",
        description_tpl="{subject}, new build.",
        var_pools={"subject": ["Computer Science"]},
        breakdown_steps_tpl=[
            "Restate what you're recommending and what data you have about user preferences",
            "Sketch the simplest possible approach (e.g. popularity-based) as a baseline before anything fancy",
            "Scaffold the script and get the baseline recommender running end to end",
            "Add a collaborative-filtering or content-based improvement once the baseline works",
            "Evaluate by holding out some known preferences and checking if they'd be recommended",
            "Clean up and note the baseline vs improved approach's results for comparison",
        ],
        vague_indices=[2],
        clarify_questions_tpl=[{"question": "What kind of items are you recommending, and what data do you have — ratings, purchase history, or something else?", "target_index": 2}],
        refine_answers_tpl=["movie recommendations, based on a ratings dataset like MovieLens"],
        refine_steps_tpl=[
            "Restate: movie recommendations using a MovieLens-style ratings dataset",
            "Sketch the simplest baseline: recommend the highest-average-rated movies overall, before anything personalized",
            "Scaffold the script and get that popularity baseline running end to end on the MovieLens data",
            "Add user-based collaborative filtering (find similar users by rating overlap) once the baseline works",
            "Evaluate by holding out some known ratings and checking if similar movies would be recommended",
            "Clean up and note the baseline vs collaborative-filtering results for comparison",
        ],
    ),
    Scenario(
        id="coding_slow_sql_query",
        intent="coding",
        title_tpl="fix a slow database query, debugging existing code",
        description_tpl=None,
        var_pools={"subject": ["Computer Science"]},
        breakdown_steps_tpl=[
            "Reproduce the slowness with the exact query and realistic data volume",
            "Run EXPLAIN (or EXPLAIN ANALYZE) on the query to see what the database is actually doing",
            "Check whether it's doing a full table scan where an index could help",
            "Add the missing index (or fix a query pattern that prevents index use, like a leading wildcard LIKE)",
            "Re-run EXPLAIN to confirm the plan actually changed, not just re-timing it",
            "Re-run the original slow case to confirm the real-world speedup",
        ],
        vague_indices=[2],
        clarify_questions_tpl=[{"question": "What did EXPLAIN show — a full table scan, or something else like a slow join?", "target_index": 2}],
        refine_answers_tpl=["a full table scan on the orders table, it's not using the customer_id index at all"],
        refine_steps_tpl=[
            "Reproduce the slowness with the exact query and realistic data volume",
            "Confirm from EXPLAIN that orders is getting a full table scan instead of using the customer_id index",
            "Check whether the query's WHERE clause matches the index (e.g. no function wrapping customer_id, no implicit type mismatch) — that mismatch usually explains why an existing index gets ignored",
            "Rewrite the query or add/fix the index so customer_id lookups can actually use it",
            "Re-run EXPLAIN to confirm the plan now uses the index instead of a full scan",
            "Re-run the original slow case to confirm the real-world speedup",
        ],
    ),
    Scenario(
        id="coding_multiplayer_networking",
        intent="coding",
        title_tpl="Build the multiplayer networking layer for my game project",
        description_tpl="{subject}, new build.",
        var_pools={"subject": ["Computer Science"]},
        breakdown_steps_tpl=[
            "Restate exactly what needs to sync between players: position, actions, or full game state",
            "Sketch the message format for client-to-server and server-to-client updates",
            "Scaffold a bare server + one client that can connect and exchange a single test message",
            "Add real position/state syncing once the basic connection works",
            "Handle a client disconnecting mid-game without crashing the server",
            "Test with 2 real clients on the same network before worrying about internet latency",
        ],
        vague_indices=[2],
        clarify_questions_tpl=[{"question": "Is this turn-based or real-time, and roughly how many players at once?", "target_index": 2}],
        refine_answers_tpl=["real-time, up to 4 players, it's a small local multiplayer game"],
        refine_steps_tpl=[
            "Restate: real-time syncing of position/state for up to 4 players",
            "Sketch a compact message format for frequent real-time updates, since 4-player real-time needs low overhead per message",
            "Scaffold a bare server + one client that can connect and exchange a single test message",
            "Add real position/state syncing at a fixed tick rate once the basic connection works, testing with 2 clients first",
            "Handle a client disconnecting mid-game without crashing the server or freezing the other 3 players",
            "Test with all 4 real clients on the same network before worrying about internet latency",
        ],
    ),
]

_post = no_post
